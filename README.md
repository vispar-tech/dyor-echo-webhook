# Webhook Server

A simple HTTP webhook server for handling DYOR events.

## Overview

This project provides a lightweight webhook server that listens for incoming DYOR events and processes them accordingly.

## Features

- HTTP server for receiving webhook requests
- DYOR event handler
- Configurable server port via settings
- Logging with loguru
